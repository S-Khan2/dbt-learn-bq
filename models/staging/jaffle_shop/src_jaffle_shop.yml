version: 2
# dbt database = BigQuery project
# dbt schema = BigQuery dataset

sources:
    - name: jaffle_shop
      database: dbt-bq-360016 
      schema: jaffle_shop
      tables:
        - name: customers
        - name: orders
          loaded_at_field: _etl_loaded_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}